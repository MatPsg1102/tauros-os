// Prisma — EXCLUSIVAMENTE migrations, seeds e relatórios administrativos
// (service-role), fora do caminho do usuário (ADR-002). Modelos entram na 6.2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
