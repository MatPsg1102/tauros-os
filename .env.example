# --- Supabase (operational path: anon key + RLS) — ADR-002/003 ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Service role (Prisma admin/seeds/reports only — never in user path) ---
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=

# --- App ---
NEXT_PUBLIC_APP_ENV=development
